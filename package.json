{
  "name": "agentspark",
  "version": "1.0.0",
  "description": "Build your AI agent team in minutes",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server --files '*.html, css/*.css, js/**/*.js' --port 3000 --no-notify",
    "serve": "npx http-server . -p 3000"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.0"
  }
}
